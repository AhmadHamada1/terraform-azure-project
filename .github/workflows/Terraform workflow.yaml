name: Terraform CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: 1.3.5  # Set the Terraform version

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan

      # Add a manual approval step using environments
      - name: Await Manual Approval
        if: success()
        run: echo "Waiting for manual approval to proceed with Terraform Apply."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        # This will pause the pipeline here until manually approved via GitHub UI.
        # GitHub will show a "Approve" button in the Actions UI.

      - name: Terraform Apply
        if: success()
        run: terraform apply -auto-approve
        env:
          ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
          ARM_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}
        # The Terraform Apply step will not execute until you approve it in GitHub UI
